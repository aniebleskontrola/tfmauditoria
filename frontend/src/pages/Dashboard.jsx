import { useEffect, useState } from "react"; import { api } from "../services/api.js";
export default function Dashboard({refresh}){const [summary,setSummary]=useState(null); useEffect(()=>{api.getResumen().then(setSummary).catch(console.error)},[refresh]); if(!summary) return <section className="card">Cargando resumen...</section>; return <section><h2>Resumen del repositorio de evidencias</h2><div className="grid"><article className="metric"><span>Total de evidencias</span><strong>{summary.total}</strong></article>{Object.entries(summary.estados).map(([estado,total])=><article className="metric" key={estado}><span>{estado}</span><strong>{total}</strong></article>)}</div><div className="card"><h3>Categorías registradas</h3><p>{summary.categorias.join(", ")}</p></div></section>}
