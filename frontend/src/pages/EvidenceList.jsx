import { useEffect,useMemo,useState } from "react"; import { api } from "../services/api.js";
export default function EvidenceList({refresh}){const [items,setItems]=useState([]); const [filter,setFilter]=useState(""); useEffect(()=>{api.getEvidencias().then(setItems).catch(console.error)},[refresh]); const filtered=useMemo(()=>{const q=filter.toLowerCase(); return items.filter(i=>[i.titulo,i.categoria,i.responsable,i.estado,i.descripcion].join(" ").toLowerCase().includes(q))},[items,filter]); return <section><h2>Repositorio de evidencias</h2><input className="search" placeholder="Filtrar por título, estado, categoría o responsable..." value={filter} onChange={e=>setFilter(e.target.value)}/><div className="list">{filtered.map(item=><article className="card evidence" key={item.id}><div><h3>{item.titulo}</h3><p>{item.descripcion}</p><small>{item.fecha} · {item.responsable}</small></div><span className="badge">{item.estado}</span></article>)}</div></section>}
